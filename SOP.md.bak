# 茉霓日报 · 制作规范 SOP v3.1.0

## 📁 项目结构

```
moni-daily/
├── index.html          ← 主站（报纸架+搜索+聊天室浮动入口）
├── daily-news.html     ← 日报统一入口（Vue 3 CDN 渲染）
├── validate_daily.py   ← 日报JSON校验修复脚本
├── update_editions.py  ← 自动更新 editions.json（带去重）
├── update_index.py     ← 自动更新 search-index.json（带去重）
├── update_sections.py  ← 自动更新板块索引文件（5个板块JSON）
├── update_comments.py ← 自动更新辣评索引 comments.json
├── publish.sh          ← 一键发布脚本（6步：校验→editions→search-index→板块索引→辣评索引→git push）
│
├── css/
│   ├── daily.css       ← 日报页面样式
│   ├── index.css       ← 主站样式（含聊天室浮动按钮）
│   └── chat.css        ← 聊天室样式（PC三栏+移动端响应式）
│
├── js/
│   ├── daily.js        ← 日报渲染逻辑（Vue app）
│   ├── index.js        ← 主站逻辑（报纸架+搜索）
│   ├── common.js       ← 聊天室公共方法（语音播报、弹窗、日期格式化等）
│   ├── chat.js         ← 聊天室主逻辑（列表加载、Tab切换）
│   ├── group.js        ← 群聊详情逻辑
│   ├── private.js      ← 私聊详情逻辑
│   └── moshuo.js       ← 吵吵逻辑（气泡云、语音播报、拖拽、洗牌）
│
├── chat/
│   ├── index.html      ← 聊天室主页面（Vue app）
│   ├── group.html      ← 群聊详情页（读 ?date=xxx）
│   ├── private.html    ← 私聊详情页（读 ?section=xxx）
│   └── moshuo.html     ← 吵吵详情页（读 ?section=xxx，随机气泡云画布）
│
├── data/
│   ├── YYYYMMDD-1.json ← 每期报纸完整数据（同天第一期也带 -1 后缀）
│   ├── YYYYMMDD-2.json
│   └── meta/           ← 自动生成的索引文件（禁止手动编辑！）
│       ├── editions.json        ← 报纸目录（10条headlines/期，脚本自动维护）
│       ├── search-index.json    ← 搜索索引（editionId为数字，脚本自动维护）
│       ├── headline.json        ← 头条板块索引
│       ├── international.json   ← 国际板块索引
│       ├── tech.json            ← 科技板块索引
│       ├── finance.json         ← 财经板块索引
│       └── entertainment.json   ← 娱乐板块索引
│       └── comments.json        ← 辣评索引（所有板块的 comment 字段聚合）
│
└── SOP.md              ← 本文件（已排除在 git 外）
```

### ⚠️ 数据文件说明

- **`data/meta/editions.json`**：报纸目录。headlines 每期 **6 条**（4条显示+2条备用）。date 格式 `"2026-04-01"`（带横杠）。month 格式 `"4月"`（去前导零）。**必须通过 `update_editions.py` 生成，禁止手动编写。**
- **`data/meta/search-index.json`**：搜索索引。editionId 为**数字**（对应 editions.json 的 id）。**必须通过 `update_index.py` 生成，禁止手动编写。**
- **`data/meta/*.json`（板块索引）**：由 publish.sh 的 Step 4-5 自动生成，按板块聚合所有文章。禁止手动编写。

### 开发环境

- `moni-daily-dev/` — 开发目录，与生产隔离，避免 cron 冲突
- 开发完成后手动同步代码到 `moni-daily/`（不同步 data/ 目录下的 JSON 文件）
- IDEAS.md 等规划文档只存在于 dev 目录

## 🔄 每日制作流程

### Step 1：采集与验证新闻

**数据源（按内容类型）：**

| 内容类型  | 来源              | URL                                                            |
| --------- | ----------------- | -------------------------------------------------------------- |
| 国际/综合 | 新浪新闻          | `https://news.sina.com.cn/`                                    |
| 科技      | IT之家 / 36氪快讯 | `https://www.ithome.com/` / `https://www.36kr.com/newsflashes` |
| 国内财经  | 新浪财经          | `https://finance.sina.com.cn/`                                 |
| 娱乐快讯  | 网易娱乐          | `https://ent.163.com/`                                         |
| 补充搜索  | 36氪搜索          | `https://www.36kr.com/search/articles/关键词`                  |

**要求：**

- 选 **12-16条**，板块数量**必须精确**：国际(3篇)、科技(3篇)、国内财经(3篇)、快讯(4-6条)
- 每条：标题、摘要(1-2句)、来源、原文URL
- **每条URL必须 web_fetch 验证**：页面标题含新闻关键词 → 用，不匹配 → 丢弃换来源
- **URL必须是具体文章页**，禁止用首页/频道页/列表页凑数
- **每条新闻必须验证日期是今天的**，禁止旧新闻混入
- **头条不能与任何板块文章重复**，头条独立于板块，另选当日最大新闻

**天气数据获取（用 Open-Meteo API，不要用 wttr.in）：**

```bash
# 一次性获取三城市天气（推荐）
curl -s "https://api.open-meteo.com/v1/forecast?latitude=22.27,22.54,23.35&longitude=113.58,114.06,116.68&current=temperature_2m,relative_humidity_2m,wind_speed_10m,weather_code&timezone=Asia/Shanghai"

# 返回 JSON 数组，顺序为：珠海、深圳、汕头
# weather_code 对照: 0=晴, 1-3=多云, 45/48=雾, 51-67=雨, 71-77=雪, 80-82=阵雨, 95-99=雷暴
```

**科技新闻获取（推荐用 IT之家 RSS）：**

```bash
# IT之家 RSS 订阅（结构化数据，含标题、链接、发布时间）
curl -s "https://www.ithome.com/rss/"
```

**数据源（按内容类型）：**

| 内容类型 | 来源 | URL/方式 |
| --------- | ----------------- | -------------------------------------------------------------- |
| 国际/综合 | 新浪新闻 | `https://news.sina.com.cn/` |
| 科技 | IT之家 RSS | `curl -s "https://www.ithome.com/rss/"`（推荐）|
| 科技备选 | 36氪快讯 | `https://www.36kr.com/newsflashes` |
| 国内财经 | 新浪财经 | `https://finance.sina.com.cn/` |
| 娱乐快讯 | 网易娱乐 | `https://ent.163.com/` |
| 补充搜索 | 36氪搜索 | `https://www.36kr.com/search/articles/关键词` |
